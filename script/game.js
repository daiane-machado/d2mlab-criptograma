
import { questions } from "./list-questions"
console.log(questions)
/* 
var tabuleiro = document.getElementById('tabuleiro');

for (var i = 0; i < palavras.length; i++) {
  var palavra = palavras[i].palavra;
  var dica = palavras[i].dica;

  var divDica = document.createElement('div');
  divDica.textContent = dica;
  tabuleiro.appendChild(divDica);

  for (var j = 0; j < palavra.length; j++) {
    var celula = document.createElement('input');
    celula.type = 'text';
    celula.maxLength = 1;
    celula.className = 'celula';
    tabuleiro.appendChild(celula);
  }
  tabuleiro.appendChild(document.createElement('br'));
}
 */